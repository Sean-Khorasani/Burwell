{
  "name": "uia_page_back",
  "description": "Navigate browser page back using Alt+Left (replaces UIA_PAGE_BACK)",
  "version": "1.0",
  "author": "Burwell Atomic UIA",
  "sequence": [
    {
      "step": 1,
      "command": "UIA_FOCUS_WINDOW",
      "parameters": {
        "hwnd": "${window}"
      },
      "metadata": {
        "description": "Focus browser window"
      }
    },
    {
      "step": 2,
      "command": "EXECUTE_SCRIPT",
      "parameters": {
        "script_path": "test_scripts/uia_send_hotkey.json",
        "variables": {
          "window": "${window}",
          "modifier": "VK_MENU",
          "key": "VK_LEFT"
        }
      },
      "metadata": {
        "description": "Navigate back (Alt+Left)"
      }
    },
    {
      "step": 3,
      "command": "WAIT",
      "parameters": {
        "duration": "500ms"
      },
      "metadata": {
        "description": "Wait for page navigation"
      }
    }
  ]
}